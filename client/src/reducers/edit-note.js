const defaultState = {
  note: {}, 
  _id: -1,
  index: -1
}

const editNoteReducer = (state = defaultState, action) => {
  switch (action.type) {
    default: 
      return state;
  }
}

export default editNoteReducer;